{
  "tasks": {
    "check": "deno check ./**/*.ts",
    "fmt": "deno fmt ./**/*.ts",
    "fmt:check": "deno fmt --check ./**/*.ts",
    "lint": "deno lint ./**/*.ts",
    "test:integration": "deno test --allow-all denops/**/*.integration.test.ts",
    "test:unit": "deno test --allow-env --allow-read"
  },
  "lock": false,
  "fmt": {
    "exclude": ["CHANGELOG.md"]
  },
  "lint": {
    "rules": {
      "exclude": [
        "no-import-prefix"
      ]
    }
  },
  "test": {
    "exclude": ["denops/**/*.integration.test.ts"]
  }
}
